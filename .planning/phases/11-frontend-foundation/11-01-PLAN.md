---
phase: 11-frontend-foundation
plan: 01
type: execute
---

<objective>
Initialize React + Vite + TypeScript project with TailwindCSS v4 and shadcn/ui component library.

Purpose: Establish the frontend project foundation with modern tooling that enables rapid, type-safe UI development.
Output: Working React project with Vite dev server, TypeScript strict mode, TailwindCSS v4 styling, and shadcn/ui components ready to use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-foundation/11-RESEARCH.md
@.planning/phases/11-frontend-foundation/11-CONTEXT.md

**Working directory for frontend:** `/Users/narekmeloyan/WebstormProjects/`
**GitHub remote:** `https://github.com/Nard248/JokesForFrontEnd.git`

**From RESEARCH.md - Standard Stack:**
- React 19.x + Vite 6.x + TypeScript 5.x
- TailwindCSS 4.x with @tailwindcss/vite plugin (CSS-first config, no JS config file)
- shadcn/ui canary (React 19 + Tailwind v4 compatible)
- clsx + tailwind-merge for class utilities

**From CONTEXT.md - User Preferences:**
- Playful, quirky personality (colors, typography to reflect this)
- Mobile-first responsive design
- Separate repo from backend

**Key decisions from prior phases:**
- Backend API at http://localhost:8000/api/v1/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize React + Vite + TypeScript project</name>
  <files>/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/package.json, vite.config.ts, tsconfig.json, src/main.tsx, src/App.tsx, index.html</files>
  <action>
Navigate to /Users/narekmeloyan/WebstormProjects/ and create new Vite project:

```bash
cd /Users/narekmeloyan/WebstormProjects
npm create vite@latest jokes-for-frontend -- --template react-ts
cd jokes-for-frontend
npm install
```

After creation:
1. Update tsconfig.json to enable strict mode and configure path aliases:
   - Add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }`
   - Ensure `"strict": true`

2. Update vite.config.ts to add path alias resolution:
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   import path from 'path'

   export default defineConfig({
     plugins: [react()],
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   })
   ```

3. Create initial project structure:
   ```
   src/
   ├── app/           # Application shell
   ├── components/    # Shared components
   │   └── ui/        # shadcn/ui components (auto-generated)
   ├── features/      # Feature modules
   ├── hooks/         # Shared hooks
   ├── lib/           # Configured libraries (axios, utils)
   ├── types/         # Shared TypeScript types
   └── main.tsx       # Entry point
   ```

4. Clean up default Vite boilerplate (remove App.css, assets folder content, simplify App.tsx)

5. Create .env.example with:
   ```
   VITE_API_URL=http://localhost:8000/api/v1
   ```

6. Create .env (gitignored) with same content for development

Do NOT install TailwindCSS yet - that's Task 2.
  </action>
  <verify>
```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend
npm run dev
# Should start dev server without errors
# Visit http://localhost:5173 should show basic React app
```
  </verify>
  <done>Vite dev server runs, TypeScript compiles without errors, path aliases work (@/ imports), project structure created</done>
</task>

<task type="auto">
  <name>Task 2: Configure TailwindCSS v4 + shadcn/ui</name>
  <files>src/index.css, vite.config.ts, components.json, tailwind.config.ts (NOT created - v4 uses CSS)</files>
  <action>
Install TailwindCSS v4 with Vite plugin (NOT PostCSS method):

```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend
npm install tailwindcss @tailwindcss/vite
```

Update vite.config.ts to add Tailwind plugin:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import path from 'path'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

Update src/index.css with Tailwind v4 CSS-first config:
```css
@import "tailwindcss";

@theme {
  /* Playful, quirky color palette */
  --color-primary: oklch(0.65 0.2 280);      /* Vibrant purple */
  --color-secondary: oklch(0.75 0.15 180);   /* Teal accent */
  --color-accent: oklch(0.85 0.18 80);       /* Warm yellow */
  --color-background: oklch(0.98 0.01 280);  /* Near white with hint */
  --color-foreground: oklch(0.15 0.02 280);  /* Near black */

  /* Fun, readable typography */
  --font-sans: "Inter", system-ui, sans-serif;
  --font-display: "Poppins", system-ui, sans-serif;

  /* Rounded, friendly corners */
  --radius-sm: 0.375rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;
  --radius-xl: 1rem;
}
```

Import index.css in main.tsx if not already imported.

Install shadcn/ui dependencies and initialize:
```bash
npm install clsx tailwind-merge class-variance-authority lucide-react
npx shadcn@canary init
```

During init, select:
- TypeScript: Yes
- Style: New York
- Base color: Neutral
- CSS variables: Yes
- Tailwind CSS config: Skip (v4 uses CSS)
- Components location: @/components
- Utils location: @/lib/utils

Create src/lib/utils.ts with cn helper:
```typescript
import { type ClassValue, clsx } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Add a test shadcn component to verify setup:
```bash
npx shadcn@canary add button
```

Update App.tsx to use the button and verify Tailwind works:
```tsx
import { Button } from '@/components/ui/button'

function App() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <div className="text-center space-y-4">
        <h1 className="text-4xl font-display font-bold text-primary">
          Jokes For
        </h1>
        <p className="text-foreground/70">
          Your daily dose of humor
        </p>
        <Button>Get Started</Button>
      </div>
    </div>
  )
}

export default App
```

IMPORTANT: Do NOT create tailwind.config.js - TailwindCSS v4 uses CSS-first configuration via @theme directive.
  </action>
  <verify>
```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend
npm run dev
# Visit http://localhost:5173
# Should see styled page with purple "Jokes For" heading and working Button component
# Check browser dev tools - no CSS errors, Tailwind classes applying
```
  </verify>
  <done>TailwindCSS v4 compiles styles, custom theme colors work, shadcn/ui Button renders correctly, cn utility available</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` completes without TypeScript errors
- [ ] TailwindCSS classes apply correctly (visible styling)
- [ ] shadcn/ui Button component renders and is interactive
- [ ] Path aliases work (@/components/ui/button imports correctly)
- [ ] Project structure matches specification
</verification>

<success_criteria>
- Vite dev server runs on http://localhost:5173
- TypeScript strict mode enabled, no type errors
- TailwindCSS v4 with custom theme colors working
- shadcn/ui initialized with Button component functional
- Project structure ready for features
- Environment variables configured for API URL
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-foundation/11-01-SUMMARY.md`
</output>
