---
phase: 11-frontend-foundation
plan: 04
type: execute
---

<objective>
Build mobile-first responsive shell layout and configure Playwright E2E testing infrastructure.

Purpose: Complete the visual foundation with a playful, engaging layout and establish testing patterns for future feature development.
Output: Polished responsive layout shell, Playwright configured with example test, project ready for Phase 12 feature development.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-foundation/11-RESEARCH.md
@.planning/phases/11-frontend-foundation/11-CONTEXT.md
@.planning/phases/11-frontend-foundation/11-01-SUMMARY.md
@.planning/phases/11-frontend-foundation/11-02-SUMMARY.md
@.planning/phases/11-frontend-foundation/11-03-SUMMARY.md

**Working directory:** `/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/`

**From CONTEXT.md - Visual Requirements:**
- Playful and engaging - not corporate, not boring
- Quirky touches and unexpected delights
- Mobile-first essential
- The shell/layout feel should establish personality foundation

**From RESEARCH.md - Testing:**
- Playwright for E2E testing
- Configure with dev server auto-start
- Use UI mode for debugging

**Design direction (from custom theme):**
- Primary: Vibrant purple (oklch 0.65 0.2 280)
- Secondary: Teal accent (oklch 0.75 0.15 180)
- Accent: Warm yellow (oklch 0.85 0.18 80)
- Font display: Poppins for headings
- Rounded corners for friendly feel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build mobile-first responsive shell layout</name>
  <files>src/components/Layout.tsx, src/components/Header.tsx, src/components/MobileNav.tsx, src/components/Footer.tsx, src/index.css</files>
  <action>
First, add Google Fonts to index.html:
```html
<!-- In index.html <head> -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
```

Update src/index.css with refined theme:
```css
@import "tailwindcss";

@theme {
  /* Playful, quirky color palette */
  --color-primary: oklch(0.65 0.2 280);       /* Vibrant purple */
  --color-primary-light: oklch(0.75 0.15 280);
  --color-primary-dark: oklch(0.5 0.2 280);

  --color-secondary: oklch(0.75 0.15 180);    /* Teal accent */
  --color-secondary-light: oklch(0.85 0.1 180);

  --color-accent: oklch(0.85 0.18 80);        /* Warm yellow */
  --color-accent-dark: oklch(0.75 0.2 80);

  --color-background: oklch(0.985 0.005 280); /* Near white with purple hint */
  --color-foreground: oklch(0.15 0.02 280);   /* Near black */

  --color-muted: oklch(0.95 0.01 280);
  --color-muted-foreground: oklch(0.45 0.02 280);

  --color-card: oklch(1 0 0);
  --color-card-foreground: oklch(0.15 0.02 280);

  --color-border: oklch(0.9 0.01 280);

  /* Fun, readable typography */
  --font-sans: "Inter", system-ui, sans-serif;
  --font-display: "Poppins", system-ui, sans-serif;

  /* Rounded, friendly corners */
  --radius-sm: 0.375rem;
  --radius-md: 0.625rem;
  --radius-lg: 1rem;
  --radius-xl: 1.5rem;
  --radius-full: 9999px;

  /* Playful shadows */
  --shadow-sm: 0 1px 2px oklch(0.15 0.02 280 / 0.05);
  --shadow-md: 0 4px 6px oklch(0.15 0.02 280 / 0.07);
  --shadow-lg: 0 10px 15px oklch(0.15 0.02 280 / 0.1);
}

/* Global styles */
body {
  font-family: var(--font-sans);
  background-color: var(--color-background);
  color: var(--color-foreground);
}

/* Smooth transitions for interactive elements */
button, a {
  transition: all 0.15s ease;
}
```

Create Header component at src/components/Header.tsx:
```tsx
import { useState } from 'react'
import { useAuth, useLogout } from '@/features/auth'
import { Button } from '@/components/ui/button'
import { Menu, X, Sparkles } from 'lucide-react'

export function Header() {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false)
  const { user, isAuthenticated, isLoading } = useAuth()
  const logoutMutation = useLogout()

  const navLinks = [
    { href: '/', label: 'Search', icon: '🔍' },
    { href: '/daily', label: 'Daily', icon: '✨' },
    { href: '/collections', label: 'Saved', icon: '💾' },
  ]

  return (
    <header className="sticky top-0 z-50 bg-card/80 backdrop-blur-md border-b border-border">
      <div className="container mx-auto px-4">
        <nav className="flex items-center justify-between h-16">
          {/* Logo */}
          <a href="/" className="flex items-center gap-2 group">
            <span className="text-2xl">😂</span>
            <span className="text-xl font-display font-bold text-primary group-hover:text-primary-dark transition-colors">
              Jokes For
            </span>
          </a>

          {/* Desktop Navigation */}
          <div className="hidden md:flex items-center gap-1">
            {navLinks.map((link) => (
              <a
                key={link.href}
                href={link.href}
                className="px-4 py-2 rounded-lg text-foreground/70 hover:text-foreground hover:bg-muted transition-all"
              >
                <span className="mr-1.5">{link.icon}</span>
                {link.label}
              </a>
            ))}
          </div>

          {/* Auth Section - Desktop */}
          <div className="hidden md:flex items-center gap-3">
            {isLoading ? (
              <div className="w-8 h-8 rounded-full bg-muted animate-pulse" />
            ) : isAuthenticated ? (
              <div className="flex items-center gap-3">
                <div className="flex items-center gap-2 px-3 py-1.5 bg-muted rounded-full">
                  <span className="w-2 h-2 bg-green-500 rounded-full" />
                  <span className="text-sm font-medium truncate max-w-32">
                    {user?.email?.split('@')[0]}
                  </span>
                </div>
                <Button
                  variant="ghost"
                  size="sm"
                  onClick={() => logoutMutation.mutate()}
                  className="text-muted-foreground hover:text-foreground"
                >
                  Logout
                </Button>
              </div>
            ) : (
              <div className="flex items-center gap-2">
                <Button variant="ghost" size="sm" asChild>
                  <a href="/login">Login</a>
                </Button>
                <Button size="sm" asChild>
                  <a href="/register" className="flex items-center gap-1">
                    <Sparkles className="w-4 h-4" />
                    Sign Up
                  </a>
                </Button>
              </div>
            )}
          </div>

          {/* Mobile Menu Button */}
          <button
            className="md:hidden p-2 -mr-2 text-foreground/70 hover:text-foreground"
            onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
            aria-label="Toggle menu"
          >
            {isMobileMenuOpen ? (
              <X className="w-6 h-6" />
            ) : (
              <Menu className="w-6 h-6" />
            )}
          </button>
        </nav>

        {/* Mobile Navigation */}
        {isMobileMenuOpen && (
          <div className="md:hidden py-4 border-t border-border">
            <div className="flex flex-col gap-1">
              {navLinks.map((link) => (
                <a
                  key={link.href}
                  href={link.href}
                  className="flex items-center gap-3 px-4 py-3 rounded-lg text-foreground/70 hover:text-foreground hover:bg-muted transition-all"
                  onClick={() => setIsMobileMenuOpen(false)}
                >
                  <span className="text-lg">{link.icon}</span>
                  <span className="font-medium">{link.label}</span>
                </a>
              ))}

              <div className="my-2 border-t border-border" />

              {isAuthenticated ? (
                <>
                  <div className="px-4 py-2 text-sm text-muted-foreground">
                    Signed in as {user?.email}
                  </div>
                  <button
                    onClick={() => {
                      logoutMutation.mutate()
                      setIsMobileMenuOpen(false)
                    }}
                    className="flex items-center gap-3 px-4 py-3 rounded-lg text-red-500 hover:bg-red-500/10 transition-all"
                  >
                    Logout
                  </button>
                </>
              ) : (
                <>
                  <a
                    href="/login"
                    className="flex items-center gap-3 px-4 py-3 rounded-lg text-foreground/70 hover:text-foreground hover:bg-muted transition-all"
                    onClick={() => setIsMobileMenuOpen(false)}
                  >
                    Login
                  </a>
                  <a
                    href="/register"
                    className="flex items-center justify-center gap-2 mx-4 py-3 rounded-lg bg-primary text-white font-medium hover:bg-primary-dark transition-all"
                    onClick={() => setIsMobileMenuOpen(false)}
                  >
                    <Sparkles className="w-4 h-4" />
                    Sign Up Free
                  </a>
                </>
              )}
            </div>
          </div>
        )}
      </div>
    </header>
  )
}
```

Create Footer component at src/components/Footer.tsx:
```tsx
export function Footer() {
  return (
    <footer className="mt-auto border-t border-border bg-muted/30">
      <div className="container mx-auto px-4 py-8">
        <div className="flex flex-col md:flex-row items-center justify-between gap-4">
          <div className="flex items-center gap-2 text-muted-foreground">
            <span className="text-xl">😂</span>
            <span className="font-display font-semibold">Jokes For</span>
            <span className="text-sm">- Find the perfect joke</span>
          </div>

          <div className="flex items-center gap-6 text-sm text-muted-foreground">
            <a href="/about" className="hover:text-foreground transition-colors">About</a>
            <a href="/privacy" className="hover:text-foreground transition-colors">Privacy</a>
            <a href="/terms" className="hover:text-foreground transition-colors">Terms</a>
          </div>

          <div className="text-sm text-muted-foreground">
            Made with ❤️ and 😂
          </div>
        </div>
      </div>
    </footer>
  )
}
```

Update Layout component at src/components/Layout.tsx:
```tsx
import { ReactNode } from 'react'
import { Header } from './Header'
import { Footer } from './Footer'

interface LayoutProps {
  children: ReactNode
}

export function Layout({ children }: LayoutProps) {
  return (
    <div className="min-h-screen flex flex-col bg-background">
      <Header />

      <main className="flex-1 container mx-auto px-4 py-6">
        {children}
      </main>

      <Footer />
    </div>
  )
}
```

Update HomePage stub to be more visually appealing at src/app/routes.tsx (just the HomePage function):
```tsx
function HomePage() {
  return (
    <div className="py-8 md:py-12">
      {/* Hero Section */}
      <div className="text-center mb-12">
        <h1 className="text-4xl md:text-5xl lg:text-6xl font-display font-bold text-foreground mb-4">
          Find Your{' '}
          <span className="text-primary">Perfect Joke</span>
        </h1>
        <p className="text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto">
          Search thousands of jokes by mood, occasion, and style.
          From dad jokes to dark humor, we've got you covered. 😏
        </p>
      </div>

      {/* Search placeholder */}
      <div className="max-w-2xl mx-auto mb-12">
        <div className="relative">
          <input
            type="text"
            placeholder="Search for jokes..."
            className="w-full px-6 py-4 text-lg rounded-xl border-2 border-border bg-card focus:border-primary focus:outline-none transition-colors"
          />
          <button className="absolute right-2 top-1/2 -translate-y-1/2 px-6 py-2 bg-primary text-white rounded-lg font-medium hover:bg-primary-dark transition-colors">
            Search
          </button>
        </div>
      </div>

      {/* Quick categories */}
      <div className="flex flex-wrap justify-center gap-3">
        {['😄 Dad Jokes', '😈 Dark Humor', '🎯 Puns', '💼 Work Appropriate', '👶 Kid Friendly'].map((tag) => (
          <button
            key={tag}
            className="px-4 py-2 rounded-full bg-muted hover:bg-muted/80 text-foreground/70 hover:text-foreground transition-all"
          >
            {tag}
          </button>
        ))}
      </div>
    </div>
  )
}
```

Install lucide-react if not already installed:
```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend
npm install lucide-react
```
  </action>
  <verify>
```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend
npm run dev
# Visit http://localhost:5173
# Visual checks:
# - Header with logo, navigation, auth state
# - Playful typography (Poppins headings)
# - Purple primary color theme
# - Hero section with search input
# - Quick category chips
# - Footer at bottom
# - Mobile: resize to <768px, verify hamburger menu works
```
  </verify>
  <done>Header, Footer, Layout components render correctly, fonts load, colors match theme, mobile menu functional</done>
</task>

<task type="auto">
  <name>Task 2: Configure Playwright E2E testing</name>
  <files>playwright.config.ts, e2e/example.spec.ts, package.json</files>
  <action>
Install Playwright:
```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend
npm install -D @playwright/test
npx playwright install chromium  # Install just Chromium for faster setup
```

Create Playwright config at playwright.config.ts:
```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',

  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },

  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'mobile',
      use: { ...devices['iPhone 13'] },
    },
  ],

  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 120 * 1000,
  },
})
```

Create e2e directory and example test at e2e/example.spec.ts:
```typescript
import { test, expect } from '@playwright/test'

test.describe('Homepage', () => {
  test('should display the hero section', async ({ page }) => {
    await page.goto('/')

    // Check hero heading
    await expect(page.getByRole('heading', { level: 1 })).toContainText('Find Your')
    await expect(page.getByRole('heading', { level: 1 })).toContainText('Perfect Joke')

    // Check search input
    await expect(page.getByPlaceholder('Search for jokes...')).toBeVisible()

    // Check quick category buttons
    await expect(page.getByRole('button', { name: /Dad Jokes/i })).toBeVisible()
  })

  test('should show header navigation', async ({ page }) => {
    await page.goto('/')

    // Check logo
    await expect(page.getByText('Jokes For')).toBeVisible()

    // Check navigation links (desktop)
    await expect(page.getByRole('link', { name: /Daily/i })).toBeVisible()
    await expect(page.getByRole('link', { name: /Saved/i })).toBeVisible()
  })

  test('should navigate to daily jokes page', async ({ page }) => {
    await page.goto('/')

    // Click Daily link
    await page.getByRole('link', { name: /Daily/i }).first().click()

    // Should be on daily page
    await expect(page).toHaveURL('/daily')
    await expect(page.getByText('Daily Joke')).toBeVisible()
  })
})

test.describe('Mobile Navigation', () => {
  test.use({ viewport: { width: 375, height: 667 } })

  test('should toggle mobile menu', async ({ page }) => {
    await page.goto('/')

    // Menu should be hidden initially
    const mobileMenu = page.locator('text=Search').first()

    // Click hamburger
    await page.getByLabel('Toggle menu').click()

    // Menu items should be visible
    await expect(page.getByRole('link', { name: /Daily/i })).toBeVisible()
  })
})

test.describe('Auth Pages', () => {
  test('login page should render without layout', async ({ page }) => {
    await page.goto('/login')

    await expect(page.getByText('Login')).toBeVisible()

    // Header should NOT be present on auth pages
    await expect(page.locator('header')).not.toBeVisible()
  })
})
```

Add test scripts to package.json:
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:headed": "playwright test --headed",
    "e2e:debug": "playwright test --debug"
  }
}
```

Create e2e/.gitkeep to ensure directory is tracked (tests will be here).
  </action>
  <verify>
```bash
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend

# Run E2E tests
npm run e2e

# Should see tests pass:
# - Homepage tests
# - Mobile navigation test
# - Auth pages test

# If any fail, check the HTML report:
# npx playwright show-report
```
  </verify>
  <done>Playwright configured, example tests pass, dev server auto-starts for tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile-first responsive shell layout with playful design and Playwright E2E tests</what-built>
  <how-to-verify>
    1. Run: `cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend && npm run dev`
    2. Visit: http://localhost:5173

    **Desktop checks (>768px):**
    3. Header: Logo with emoji, navigation links (Search, Daily, Saved), Login/Sign Up buttons
    4. Hero: Large playful heading "Find Your Perfect Joke" with purple accent
    5. Search: Large search input with Search button
    6. Categories: Emoji-prefixed filter chips (Dad Jokes, Dark Humor, etc.)
    7. Footer: Logo, links, "Made with ❤️ and 😂"
    8. Typography: Poppins for headings (chunky, playful), Inter for body

    **Mobile checks (<768px):**
    9. Resize browser to mobile width
    10. Header should show hamburger menu instead of nav links
    11. Click hamburger - menu slides down with nav links
    12. Menu should have Login option and prominent Sign Up button
    13. Footer stacks vertically

    **Overall vibe:**
    14. Does it feel playful and engaging, not corporate?
    15. Are colors vibrant (purple primary, teal accents)?
    16. Do rounded corners give friendly feel?

    **E2E tests:**
    17. Run: `npm run e2e` (should pass)
    18. Optionally: `npm run e2e:ui` to see visual test runner
  </how-to-verify>
  <resume-signal>Type "approved" if shell looks good, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` completes without errors
- [ ] Layout renders correctly on desktop and mobile
- [ ] Header navigation works
- [ ] Mobile hamburger menu toggles
- [ ] Fonts (Poppins, Inter) load correctly
- [ ] Custom theme colors apply
- [ ] Footer sticks to bottom of page
- [ ] Playwright tests pass (`npm run e2e`)
</verification>

<success_criteria>
- Responsive shell layout complete with Header, Footer, Layout components
- Playful visual design with custom colors and typography
- Mobile-first responsive behavior working
- Playwright E2E infrastructure configured with passing tests
- Ready for Phase 12: Frontend Features
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-foundation/11-04-SUMMARY.md`

If this is the last plan (plan 04 of 04):
- Mark "Phase 11: Frontend Foundation COMPLETE" in summary
- Note readiness for Phase 12
</output>
