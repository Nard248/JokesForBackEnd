---
phase: 12-frontend-features
plan: 01
type: execute
---

<objective>
Connect the homepage search UI to the backend API, displaying real joke results.

Purpose: Transform the static homepage into a functional search engine - the core value proposition.
Output: Working search with real results from backend, filter buttons functional, loading/error states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-foundation/11-04-SUMMARY.md

**Key files:**
@/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/src/lib/api.ts
@/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/src/app/routes.tsx

**Tech stack available:**
- TanStack Query for server state
- Axios with JWT interceptors
- React Router 7.x
- TailwindCSS v4 + shadcn/ui
- lucide-react icons

**Established patterns:**
- Feature-based organization (src/features/)
- TanStack Query hooks in feature api.ts files
- Zustand for client state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jokes feature with TanStack Query hooks</name>
  <files>
    src/features/jokes/index.ts,
    src/features/jokes/api.ts,
    src/features/jokes/types.ts
  </files>
  <action>
Create jokes feature module:

1. **types.ts** - Re-export types from lib/api.ts (Joke, JokeSearchParams, PaginatedResponse)

2. **api.ts** - Create TanStack Query hooks:
   - `useJokeSearch(params)` - useQuery with queryKey ['jokes', 'search', params], calls jokesApi.search
   - `useRandomJoke()` - useQuery with queryKey ['jokes', 'random'], calls jokesApi.getRandom
   - `jokeKeys` - Query key factory following TanStack patterns

Use keepPreviousData: true for search to prevent flash during pagination.
Enable the query only when there are actual search params (not on empty homepage).

3. **index.ts** - Export hooks and types
  </action>
  <verify>TypeScript compiles: cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend && npm run build</verify>
  <done>Jokes feature created with search and random hooks, types exported</done>
</task>

<task type="auto">
  <name>Task 2: Build SearchPage component with API integration</name>
  <files>
    src/pages/SearchPage.tsx,
    src/components/JokeCard.tsx,
    src/components/SearchFilters.tsx
  </files>
  <action>
1. **JokeCard.tsx** - Reusable joke display component:
   - Show joke text (or setup/punchline if two-parter)
   - Display format, age_rating badges
   - Show tone tags as colored chips
   - Placeholder for save button (just icon, no functionality yet)
   - Placeholder for rating (thumbs icons, no functionality yet)
   - Responsive: full width on mobile, card layout on desktop

2. **SearchFilters.tsx** - Filter bar component:
   - Search input (controlled, debounced 300ms)
   - Category buttons from homepage (Dad Jokes → tones=dad-joke, etc.)
   - Age rating dropdown (kid-safe, teen, adult, family-friendly)
   - Clear filters button
   - Pass filter state up via onChange prop

3. **SearchPage.tsx** - Main search results page:
   - Use useJokeSearch hook with filter state
   - Display SearchFilters at top
   - Show loading skeleton while fetching
   - Show results grid (responsive: 1 col mobile, 2 cols tablet, 3 cols desktop)
   - Show empty state if no results
   - Show error state with retry button
   - Pagination at bottom (simple prev/next, page count)

Wire category buttons on homepage to navigate to /search?tones=dad-joke etc.
  </action>
  <verify>
Start dev server, search for jokes, verify results display from backend:
cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend && npm run dev
Then visit http://localhost:5173/search?q=test
  </verify>
  <done>Search page displays real jokes from API with filters, loading states, pagination</done>
</task>

<task type="auto">
  <name>Task 3: Connect homepage to search and add routes</name>
  <files>
    src/app/routes.tsx,
    src/pages/index.ts
  </files>
  <action>
1. Update routes.tsx:
   - Import SearchPage
   - Add route: { path: 'search', element: <SearchPage /> }
   - Keep HomePage for landing, but wire search input to navigate to /search
   - Move HomePage component to src/pages/HomePage.tsx for better organization

2. Update homepage search behavior:
   - On form submit or search button click → navigate to /search?q={query}
   - Category buttons → navigate to /search?tones={slug}
   - "Try:" suggestions → navigate to /search?q={suggestion}

3. Create src/pages/index.ts barrel export for all pages

4. Test the full search flow:
   - Homepage → type query → search → see results
   - Homepage → click category → filtered results
  </action>
  <verify>
npm run build passes
Navigate from homepage to search with query in URL
Search results appear from backend
  </verify>
  <done>Homepage connected to search, category buttons work, full search flow functional</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Search page loads at /search
- [ ] Typing query and clicking search shows results from backend
- [ ] Category buttons filter correctly
- [ ] Loading, error, and empty states work
- [ ] Pagination works (if enough jokes in DB)
- [ ] Mobile responsive layout works
</verification>

<success_criteria>
- All tasks completed
- Search page fetches real data from backend API
- Filters work (query, tones)
- Loading/error/empty states render correctly
- Responsive design works on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-features/12-01-SUMMARY.md`
</output>
