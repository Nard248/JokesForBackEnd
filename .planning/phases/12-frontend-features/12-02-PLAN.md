---
phase: 12-frontend-features
plan: 02
type: execute
---

<objective>
Build authentication UI with login and registration forms that connect to the backend.

Purpose: Enable user accounts - prerequisite for protected features (collections, ratings, preferences).
Output: Working login/register pages, proper auth state display in header, redirect after auth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-frontend-features/12-01-SUMMARY.md

**Key files:**
@/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/src/features/auth/api.ts
@/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/src/features/auth/store.ts
@/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/src/components/Header.tsx

**Existing auth infrastructure:**
- useLogin, useRegister, useLogout mutations in auth/api.ts
- useAuthStore with user/token state
- AuthProvider checks session on mount
- Header shows login/logout based on isAuthenticated

**Decisions:**
- Email-only login (no username)
- HttpOnly cookies for JWT storage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginPage with form and API integration</name>
  <files>
    src/pages/LoginPage.tsx,
    src/components/ui/input.tsx
  </files>
  <action>
1. Add shadcn Input component if not present:
   npx shadcn@canary add input

2. **LoginPage.tsx** - Full login page:
   - Centered card layout with logo
   - Email input (type="email", required)
   - Password input (type="password", required)
   - "Login" button with loading state
   - Link to register page
   - Error message display (invalid credentials, network error)

3. Form handling:
   - Use React useState for form values
   - On submit, call useLogin mutation
   - On success: redirect to "/" (or returnTo if in URL params)
   - On error: display error message (parse from API response)

4. Styling:
   - Use existing playful theme (purple primary)
   - Card with shadow, rounded corners
   - Responsive (full width on mobile, max-w-md centered on desktop)
  </action>
  <verify>npm run build passes, login page renders at /login</verify>
  <done>Login page with email/password form, connects to backend auth API</done>
</task>

<task type="auto">
  <name>Task 2: Create RegisterPage with form and validation</name>
  <files>
    src/pages/RegisterPage.tsx
  </files>
  <action>
1. **RegisterPage.tsx** - Full registration page:
   - Centered card layout matching login
   - Email input
   - Password input with requirements hint (8+ chars)
   - Confirm password input
   - "Create Account" button with loading state
   - Link to login page
   - Error message display

2. Form handling:
   - Client-side validation: passwords match, email format, password length
   - On submit, call useRegister mutation with email, password1, password2
   - On success: redirect to "/" (user is automatically logged in)
   - On error: display API error (email taken, password too weak, etc.)

3. UX polish:
   - Show password toggle (eye icon)
   - Disable submit while loading
   - Clear errors on input change
  </action>
  <verify>npm run build passes, register page at /register, form validates</verify>
  <done>Register page with form validation, connects to backend registration API</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Login and registration forms with full API integration:
- Login at /login with email/password
- Register at /register with email/password/confirm
- Header updates to show user when logged in
- Logout button works
  </what-built>
  <how-to-verify>
1. Start backend: cd /Users/narekmeloyan/PycharmProjects/JokesForProject && python manage.py runserver
2. Start frontend: cd /Users/narekmeloyan/WebstormProjects/jokes-for-frontend && npm run dev
3. Visit http://localhost:5173/register
4. Register a new account (use a test email)
5. Verify you're redirected to homepage and header shows your username
6. Click logout in header
7. Visit http://localhost:5173/login
8. Login with the account you just created
9. Verify header shows username again
10. Try invalid login - should show error message
  </how-to-verify>
  <resume-signal>Type "approved" if auth flow works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Login page renders and accepts input
- [ ] Register page renders with password confirmation
- [ ] Successful login redirects and shows user in header
- [ ] Successful register redirects and shows user in header
- [ ] Invalid credentials show error message
- [ ] Logout works and clears auth state
</verification>

<success_criteria>
- All tasks completed
- Login form works with backend
- Register form works with backend
- Auth state persists (refresh page, still logged in)
- Error handling works for invalid credentials
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-features/12-02-SUMMARY.md`
</output>
