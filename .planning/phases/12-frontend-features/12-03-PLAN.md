---
phase: 12-frontend-features
plan: 03
type: execute
---

<objective>
Build the Daily Joke feature page with personalized joke display and history.

Purpose: Daily engagement hook - users return daily for personalized humor.
Output: Daily joke page showing today's joke (personalized if logged in), history view.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-frontend-features/12-02-SUMMARY.md

**Key files:**
@/Users/narekmeloyan/WebstormProjects/jokes-for-frontend/src/lib/api.ts

**Daily joke API:**
- dailyJokeApi.getToday() → { joke: Joke, date: string }
- dailyJokeApi.getHistory() → PaginatedResponse<{ joke: Joke, date: string }>

**Note:** Daily joke works for both anonymous and authenticated users:
- Anonymous: Gets a random recommendation
- Authenticated: Gets personalized based on preferences
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create daily-joke feature with TanStack Query hooks</name>
  <files>
    src/features/daily-joke/index.ts,
    src/features/daily-joke/api.ts
  </files>
  <action>
1. **api.ts** - Create TanStack Query hooks:
   - `useTodaysJoke()` - useQuery with queryKey ['daily-joke', 'today'], calls dailyJokeApi.getToday
   - `useDailyJokeHistory()` - useQuery with queryKey ['daily-joke', 'history'], calls dailyJokeApi.getHistory
   - `dailyJokeKeys` - Query key factory

Configure useTodaysJoke with:
- staleTime: 1000 * 60 * 60 (1 hour - joke changes daily, not per request)
- refetchOnWindowFocus: false

2. **index.ts** - Export hooks
  </action>
  <verify>npm run build passes</verify>
  <done>Daily joke feature hooks created</done>
</task>

<task type="auto">
  <name>Task 2: Build DailyJokePage with today's joke display</name>
  <files>
    src/pages/DailyJokePage.tsx,
    src/components/DailyJokeCard.tsx
  </files>
  <action>
1. **DailyJokeCard.tsx** - Featured joke display component:
   - Large, centered card design (hero-like)
   - Date display at top ("Today's Joke - January 12, 2026")
   - Joke text prominently displayed (larger font)
   - If two-parter: show setup, then reveal punchline button (or auto-reveal)
   - Tone/format badges below joke
   - Action buttons: Save, Share, Rate (placeholders for now)
   - Playful styling with gradient border or shadow

2. **DailyJokePage.tsx** - Daily joke page:
   - Use useTodaysJoke() hook
   - Loading state: animated skeleton
   - Error state: friendly message + retry
   - Display DailyJokeCard with joke data
   - Section below: "Previous Daily Jokes" (history)
   - History as smaller cards in a list/grid
   - Use useDailyJokeHistory() for history

3. Layout considerations:
   - Mobile: Stack vertically
   - Desktop: Today's joke prominent, history in scrollable grid below
  </action>
  <verify>
Start dev server, visit /daily, verify today's joke loads from API
npm run dev, visit http://localhost:5173/daily
  </verify>
  <done>Daily joke page displays today's joke from API, shows history</done>
</task>

<task type="auto">
  <name>Task 3: Wire up routes and navigation</name>
  <files>
    src/app/routes.tsx,
    src/pages/index.ts
  </files>
  <action>
1. Update routes.tsx:
   - Import DailyJokePage from pages
   - Update daily route to use new component
   - Ensure page is in Layout wrapper

2. Update pages/index.ts:
   - Export DailyJokePage

3. Verify header navigation:
   - "Daily" link in header should navigate to /daily
   - Active state styling for current route

4. Test flow:
   - Homepage → click Daily in header → Daily joke page loads
   - Joke displays from backend (or error if backend not running)
  </action>
  <verify>
npm run build passes
Navigation from header to /daily works
Daily joke displays from API
  </verify>
  <done>Daily joke page integrated into app routing, navigation works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] /daily route loads DailyJokePage
- [ ] Today's joke fetched from API and displayed
- [ ] Loading skeleton shows while fetching
- [ ] History section shows previous daily jokes
- [ ] Header "Daily" link navigates correctly
</verification>

<success_criteria>
- All tasks completed
- Daily joke page shows real joke from backend
- History section shows past daily jokes
- Loading and error states work
- Navigation from header works
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-features/12-03-SUMMARY.md`
</output>
