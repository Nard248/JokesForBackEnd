---
phase: 12-frontend-features
plan: 06
type: execute
---

<objective>
Implement Save/Unsave joke functionality and integrate into joke cards throughout the app.

Purpose: Allow users to save jokes to their collections for later retrieval.
Output: Save button on joke cards, collection picker, saved state visible, unsave works.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-frontend-features/12-05-SUMMARY.md

**SavedJokes API (from lib/api.ts):**
- savedJokesApi.save(jokeId, collectionId, note?) → SavedJoke
- savedJokesApi.unsave(savedJokeId) - DELETE
- savedJokesApi.list() → PaginatedResponse<SavedJoke>
- savedJokesApi.search(params) → search within saved jokes

**SavedJoke type:**
{ id, joke: Joke, collection: number, note: string | null, saved_at: string }

**PROTECTED:** Save requires authentication.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create saved-jokes feature with hooks</name>
  <files>
    src/features/saved-jokes/index.ts,
    src/features/saved-jokes/api.ts
  </files>
  <action>
1. **api.ts** - Create TanStack Query hooks:
   - `useSavedJokes()` - useQuery with queryKey ['saved-jokes'], calls savedJokesApi.list
   - `useSaveJoke()` - useMutation for saving
     - On success: invalidate ['saved-jokes'], ['collections'] (joke_count changes)
   - `useUnsaveJoke()` - useMutation for unsaving
     - On success: invalidate ['saved-jokes'], ['collections', collectionId, 'jokes']
   - `savedJokeKeys` - Query key factory

2. Create helper to check if joke is already saved:
   - Extract joke IDs from saved jokes list
   - Provide as Set for O(1) lookup

3. **index.ts** - Export hooks
  </action>
  <verify>npm run build passes</verify>
  <done>Saved jokes feature hooks created</done>
</task>

<task type="auto">
  <name>Task 2: Build SaveButton component with collection picker</name>
  <files>
    src/components/SaveButton.tsx,
    src/components/CollectionPicker.tsx
  </files>
  <action>
1. Add shadcn Popover component:
   npx shadcn@canary add popover

2. **CollectionPicker.tsx** - Dropdown to select collection:
   - Use useCollections() to list collections
   - Radio or checkbox list of collection names
   - "Favorites" at top (is_default)
   - Quick "New Collection" option at bottom
   - On select: calls onSelect(collectionId)

3. **SaveButton.tsx** - Save/Unsave button:
   Props: jokeId: number, savedJokeId?: number (if already saved)

   States:
   - Not saved: Bookmark outline icon, "Save"
   - Saved: Bookmark filled icon, "Saved" with checkmark
   - Loading: Pulse animation

   Behavior:
   - If not authenticated: redirect to /login
   - If not saved: click opens CollectionPicker popover
     - After selecting collection: call useSaveJoke mutation
     - Default to Favorites if only one click needed
   - If saved: click unsaves (with confirmation?) or shows "Saved to X" with option to unsave

4. UX consideration:
   - Quick save (one click to Favorites)
   - Long press or dropdown for collection choice
   - Or: Always show collection picker (cleaner)
  </action>
  <verify>npm run build passes, SaveButton renders</verify>
  <done>SaveButton component with collection picker, auth-aware</done>
</task>

<task type="auto">
  <name>Task 3: Integrate SaveButton into joke cards and add saved indicator</name>
  <files>
    src/components/JokeCard.tsx,
    src/components/DailyJokeCard.tsx,
    src/hooks/useSavedJokesLookup.ts
  </files>
  <action>
1. Create **useSavedJokesLookup.ts** hook:
   - Fetches saved jokes list
   - Returns: { isSaved(jokeId), getSavedJokeId(jokeId), isLoading }
   - Use Map for efficient lookup: jokeId → savedJokeId

2. Update **JokeCard.tsx**:
   - Use useSavedJokesLookup to check if joke is saved
   - Pass savedJokeId to SaveButton if saved
   - Add SaveButton to card actions area
   - Show subtle "Saved" indicator if in collection

3. Update **DailyJokeCard.tsx**:
   - Same integration as JokeCard
   - More prominent save button in action row

4. Test flow:
   - Login
   - Go to search, find a joke
   - Click Save button → picker opens → select Favorites
   - Button changes to "Saved"
   - Go to /collections, click Favorites
   - Joke appears in collection
   - Click unsave on the joke
   - Joke removed from collection
  </action>
  <verify>
npm run build passes
Login, save a joke, verify it appears in Favorites collection
Unsave the joke, verify it's removed
  </verify>
  <done>Save functionality integrated into all joke cards, works end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Save button appears on JokeCard
- [ ] Save button appears on DailyJokeCard
- [ ] Clicking Save (when logged in) opens collection picker
- [ ] Saving adds joke to selected collection
- [ ] Saved state reflected in UI
- [ ] Unsave removes joke from collection
- [ ] Saved jokes appear in collection detail view
</verification>

<success_criteria>
- All tasks completed
- Save/Unsave works with backend
- Collection picker allows choosing collection
- Saved state visible on joke cards
- Changes reflect immediately in collections page
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-features/12-06-SUMMARY.md`
</output>
