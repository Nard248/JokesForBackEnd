---
phase: 12-frontend-features
plan: 07
type: execute
---

<objective>
Build Settings page with user preferences and account management.

Purpose: Allow users to customize their experience and manage account settings.
Output: Settings page with preferences display/edit, account info, logout option.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-frontend-features/12-06-SUMMARY.md

**Preferences API:**
Need to add to lib/api.ts:
- GET /api/v1/preferences/me/ → UserPreference
- PATCH /api/v1/preferences/me/ → update preferences

**UserPreference type (from backend):**
{
  preferred_tones: Array<{id, name, slug}>,
  preferred_contexts: Array<{id, name, slug}>,
  preferred_age_rating: {id, name, slug, min_age} | null,
  preferred_language: {id, name, code} | null,
  onboarding_completed: boolean
}

**PROTECTED:** Settings page requires authentication.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preferences API and create feature hooks</name>
  <files>
    src/lib/api.ts,
    src/features/preferences/index.ts,
    src/features/preferences/api.ts
  </files>
  <action>
1. Update **lib/api.ts** - Add preferences types and API:

   Types:
   - UserPreference interface with fields from backend

   API:
   - preferencesApi.getMe() → UserPreference
   - preferencesApi.updateMe(data) → UserPreference

2. **api.ts** - Create TanStack Query hooks:
   - `usePreferences()` - useQuery with queryKey ['preferences', 'me']
   - `useUpdatePreferences()` - useMutation
     - On success: invalidate ['preferences', 'me']
   - `preferencesKeys` - Query key factory

3. **index.ts** - Export hooks
  </action>
  <verify>npm run build passes</verify>
  <done>Preferences API and hooks created</done>
</task>

<task type="auto">
  <name>Task 2: Build SettingsPage with preferences and account sections</name>
  <files>
    src/pages/SettingsPage.tsx,
    src/components/PreferencesForm.tsx
  </files>
  <action>
1. Add shadcn components for form:
   npx shadcn@canary add select checkbox label

2. **PreferencesForm.tsx** - Editable preferences form:
   - Preferred age rating: Select dropdown
   - Preferred tones: Multi-select checkboxes
   - Preferred contexts: Multi-select checkboxes
   - Save button with loading state
   - Success toast on save
   - Note: These preferences affect daily joke recommendations

3. **SettingsPage.tsx** - Main settings page:
   Sections:

   a) Account Info:
      - Email display (from useAuth user)
      - Member since (if available)
      - Logout button (prominent)

   b) Preferences:
      - PreferencesForm component
      - Fetched from usePreferences()

   c) Danger Zone:
      - Future: Delete account (stub for now)

   Layout:
   - Card-based sections
   - Responsive: stack on mobile, sidebar+content on desktop (optional)
   - Use protected route pattern

4. Protected route check:
   - Use ProtectedRoute component from 12-05
  </action>
  <verify>npm run build passes, settings page renders</verify>
  <done>Settings page with account info and preferences form</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Settings page with:
- Account section showing email and logout
- Preferences form with age rating, tones, contexts
- Save preferences updates backend
  </what-built>
  <how-to-verify>
1. Start backend: python manage.py runserver
2. Start frontend: npm run dev
3. Login with a test account
4. Navigate to /settings (or click Settings in header)
5. Verify account email is shown
6. Change a preference (e.g., select different age rating)
7. Click Save
8. Refresh page - verify preference persisted
9. Go to /daily - daily joke should reflect preferences (eventually)
10. Click Logout - verify redirected to homepage and logged out
  </how-to-verify>
  <resume-signal>Type "approved" if settings work, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Settings page loads at /settings
- [ ] Account info displays correctly
- [ ] Preferences form loads current preferences
- [ ] Saving preferences updates backend
- [ ] Logout works
- [ ] Protected route redirects unauthenticated users
</verification>

<success_criteria>
- All tasks completed
- Settings page functional with real backend
- Preferences persist across sessions
- Logout works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-features/12-07-SUMMARY.md`
</output>
