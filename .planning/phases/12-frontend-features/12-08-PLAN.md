---
phase: 12-frontend-features
plan: 08
type: execute
---

<objective>
Add sharing functionality, polish the UI, update E2E tests, and complete MVP features.

Purpose: Complete the MVP with sharing (key viral feature) and ensure quality.
Output: Share functionality, polished UI, comprehensive E2E tests, MVP complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-frontend-features/12-07-SUMMARY.md

**Share API (from lib/api.ts):**
- POST /api/v1/jokes/{id}/share/ - Records share event, returns { share_url }
- Joke has share_image_url field for share cards

**Share approach:**
- Native Web Share API if available (mobile)
- Fallback: Copy to clipboard
- Track share events via API
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ShareButton component with native share and clipboard</name>
  <files>
    src/components/ShareButton.tsx,
    src/features/jokes/api.ts
  </files>
  <action>
1. Add share mutation to **jokes/api.ts**:
   - `useShareJoke()` - useMutation calling POST /jokes/{id}/share/
   - Returns share URL

2. **ShareButton.tsx** - Share button component:
   Props: joke: Joke

   Behavior:
   - On click: call useShareJoke mutation to record event
   - Check navigator.share availability
   - If available (mobile): Use Web Share API with:
     - title: "Check out this joke!"
     - text: joke.text (truncated if long)
     - url: share URL from API response
   - If not available (desktop): Copy share URL to clipboard
     - Show toast: "Link copied to clipboard!"

   UI:
   - Share icon (lucide-react Share2)
   - Tooltip or label "Share"
   - Loading state while recording share

3. Add shadcn Tooltip and Toast if not present:
   npx shadcn@canary add tooltip sonner
   (sonner is the toast library shadcn uses)

4. Set up Toaster in App.tsx or Providers
  </action>
  <verify>npm run build passes</verify>
  <done>ShareButton component with native share and clipboard fallback</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ShareButton into joke cards and polish UI</name>
  <files>
    src/components/JokeCard.tsx,
    src/components/DailyJokeCard.tsx,
    src/app/providers/index.tsx
  </files>
  <action>
1. Add Toaster to providers:
   - Import Toaster from sonner
   - Add <Toaster /> to Providers component

2. Update **JokeCard.tsx**:
   - Add ShareButton to action row (alongside Save and Rating)
   - Layout actions nicely: Rate left, Share center, Save right (or similar)

3. Update **DailyJokeCard.tsx**:
   - Add ShareButton with more prominence
   - "Share today's joke" as accessible label

4. UI polish pass:
   - Ensure consistent spacing across cards
   - Check mobile responsiveness of action buttons
   - Verify hover/focus states on all interactive elements
   - Check dark mode compatibility if supported (or note as future)

5. Quick responsiveness check:
   - Test at 375px (mobile)
   - Test at 768px (tablet)
   - Test at 1024px+ (desktop)
  </action>
  <verify>npm run build passes, share button visible on cards</verify>
  <done>ShareButton integrated, UI polished across breakpoints</done>
</task>

<task type="auto">
  <name>Task 3: Update E2E tests for new features</name>
  <files>
    e2e/example.spec.ts,
    e2e/search.spec.ts,
    e2e/auth.spec.ts
  </files>
  <action>
1. Create **e2e/search.spec.ts** - Search functionality tests:
   - Homepage search submits and navigates to /search
   - Search page displays results
   - Category filter buttons work
   - Pagination works (if testable)

2. Create **e2e/auth.spec.ts** - Authentication tests:
   - Login form accepts input
   - Invalid login shows error
   - Protected route redirects to login
   - (Skip actual login test - would need test user in DB)

3. Update **e2e/example.spec.ts**:
   - Update any tests broken by new components
   - Add test for daily joke page load
   - Add test for navigation to collections (redirects if not logged in)

4. Run tests:
   npm run e2e

5. Fix any failing tests due to UI changes
  </action>
  <verify>npm run e2e passes (or shows expected failures for auth-required features)</verify>
  <done>E2E tests updated for new features, test suite passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete MVP frontend with:
- Search with API integration and filters
- Authentication (login/register)
- Daily joke with history
- Rating functionality
- Collections management
- Save jokes to collections
- Settings with preferences
- Share functionality
- E2E test coverage
  </what-built>
  <how-to-verify>
1. Start backend: python manage.py runserver
2. Start frontend: npm run dev

**Public flows:**
3. Homepage loads with creative design
4. Search for "dad" - see results from API
5. Click a category button - filtered results
6. Visit /daily - see today's joke

**Auth flows:**
7. Click Login, create account or use existing
8. After login, header shows username

**Protected flows:**
9. Rate a joke (thumbs up/down) - persists
10. Save a joke to Favorites
11. Go to /collections - see Favorites with saved joke
12. Create new collection, save another joke to it
13. Go to /settings - see preferences, make a change, save
14. Share a joke - copy to clipboard or native share

**Final checks:**
15. Mobile responsive (resize browser)
16. Logout works
17. No console errors

**E2E tests:**
18. Run npm run e2e - all should pass
  </how-to-verify>
  <resume-signal>Type "approved" if MVP is complete, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Share functionality works
- [ ] All joke cards have Save, Rate, Share buttons
- [ ] Toast notifications work
- [ ] E2E tests pass
- [ ] Mobile responsive
- [ ] No console errors in dev tools
</verification>

<success_criteria>
- All tasks completed
- Share functionality works with backend
- E2E tests cover new features
- MVP feature-complete
- Phase 12 complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-features/12-08-SUMMARY.md`

Final summary should note:
- **PHASE 12: FRONTEND FEATURES - COMPLETE**
- **MILESTONE 1: MVP LAUNCH - COMPLETE**
- List all features implemented
- Note any known issues or future improvements
</output>
